<?xml version="1.0" encoding="UTF-8" ?>
<!--
  gp_crack_test — Isolated cracker test node
  No proxy. No firewall. Single port closed.

  Test sequence:
    Step 1 — vanilla baseline:
      connect 10.0.0.20
      SSHcrack
      → red-to-green matrix, ~10s, port 22 opens

    Step 2 — V2 animation (reset node first by reloading extension):
      connect 10.0.0.20
      SSHcrack_v2
      → red-to-ORANGE matrix, ~10s, port 22 opens
      → LogOutput: TIMER STARTED, TIMER DONE, PORT VERIFY isOpen=true

  Both crackers target the same vanilla port 22.
  Run them on separate sessions (reload between tests) so port isn't already open.
-->
<Computer id="gp_crack_test"
          name="GP Crack Test"
          ip="10.0.0.20"
          security="1"
          allowsDefaultBootModule="false"
          type="1">

  <adminPass pass="admin" />

  <!-- Vanilla SSH port — target for SSHcrack AND SSHcrack_v2 -->
  <ports>22</ports>

  <!-- 1 port needed to porthack — keeps test loop short -->
  <portsForCrack val="1" />

  <!-- Long trace — won't interrupt testing -->
  <trace time="9999" />

  <file path="home" name="README.txt">GP Crack Test Node
===================
Step 1 (vanilla baseline):
  SSHcrack
  Expect: red-to-green matrix, ~10s, port 22 opens

Step 2 (V2 — reload extension first to reset port state):
  SSHcrack_v2
  Expect: red-to-ORANGE matrix, ~10s, port 22 opens
  Check LogOutput.log for:
    [GP] TIMER STARTED
    [GP] TIMER DONE
    [GP] PORT VERIFY: SSHcrack_v2 isOpen=true
</file>

</Computer>
